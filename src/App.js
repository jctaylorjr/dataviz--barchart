const width = 960;
const height = 500;
const circleX = width / 2;
const circleY = height / 2;

const App = () => (
	<svg>
		<circle cx={circleX} cy={circleY}></circle>
	</svg>
);

export default App;
